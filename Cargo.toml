[package]
name = "visualizing-crossbeam-epoch"
version = "0.1.0"
edition = "2024"

[dependencies]
crossbeam-epoch = "0.9.21"

[target.'cfg(loom)'.dependencies]
loom = "0.7"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(loom)'] }